---
applications:
  - name: calculator
    path: build/libs/calculator.jar
    random-route: true
    env:
      WELCOME_MESSAGE: Hello from the calculator review environment
      JBP_CONFIG_OPEN_JDK_JRE: '{ jre: { version: 11.+ } }'